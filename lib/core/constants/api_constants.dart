class ApiConstants {
  // Local dev server on your PC's LAN IP. Phone and PC must be on the
  // same Wi-Fi network. Started with: dart_frog dev --host 0.0.0.0
  static const String baseUrl = 'http://192.168.1.6:8080';

// Swap back to this before pushing / shipping a release build:
// static const String baseUrl = 'https://robox-backend-2.onrender.com';
}